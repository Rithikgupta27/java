package utils;

import java.util.HashMap;
import java.util.Map;
import static utils.TaskManagerValidationRule.*;
import taskManager.TaskManager;

public class Populate {
Map<Integer,TaskManager> task=new HashMap<>();

//task.put(TaskManager.getId(),TaskManager validateAllInput());
